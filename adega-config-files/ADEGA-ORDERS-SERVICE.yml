eureka:
  client:
    defaultZone: http://localhost:8761
    fetch-register: true
    register-with-eureka: true

server:
  port: 7070

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/adega-orders-db
  jpa:
    hibernate:
      ddl-auto: update
